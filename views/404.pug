doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    title SYSTEM ERROR
  body(style="background-color: #000; color: #00ff00; padding: 50px; font-family: monospace; line-height: 1.6;")
    h1(style="color: #ff0033; border-bottom: 2px solid #ff0033; padding-bottom: 10px;") [WARNING] INVALID REQUEST METHOD DETECTED
    p > サーバーへの不正なアクセス（未対応メソッド）を検知しました。
    p > リクエストは安全のため破棄されました。
    p > 落ち着いて、正規のルート（GET/POST）から再度アプローチしてください。
    br
    p [ SYSTEM: "君のやり方は合理的ではない。やり直せ" ]